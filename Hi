Please modify my BigQuery SQL based on these review comments.

Context:
The ETL pattern is RAW → INTAKE → STAGING → TARGET.
This file is for the STAGING load: S_RXBNFT_CAREMARK_PA.

Required changes:

1. Do not insert into any master key/reference tables.
2. Use MBR_ACCT_MK only as a lookup to get MBR_ACCT_GID.
3. Remove any NOT EXISTS logic used for creating/checking missing member keys.
4. Add SRC_CD = 'X' filter wherever member/client lookup tables are used.
5. If source code is selected into staging, hardcode it as 'X' AS SRC_CD.
6. In client lookup, keep only LVL1_ACCT_GID unless LVL2/LVL3 are required by the target table.
7. Remove LVL2_ACCT_GID > 0 type filters; replace with SRC_CD = 'X'.
8. Derive ACTN_TYP_CD in staging by comparing incoming stage-build data with the TARGET table, not the staging table.
9. Use EVENT_ID as the business key for target comparison.
10. Do not use MBR_ACCT_FULL_ID as the key because it is not unique.
11. ACTN_TYP_CD logic should be:
   - If current target record exists for EVENT_ID → 'U'
   - Else → 'I'
12. Target lookup should use:
   TARGET.EVENT_ID = V.EVENT_ID
   AND TARGET.CURR_IND = 'Y'
13. Apply ICD period formatting for PRIMARY_ICD_CODE and ICD_CD2 through ICD_CD9:
   - If null, length <= 3, or already contains '.', keep as-is.
   - Else insert period after the 3rd character.
14. Keep REC_ADD_TS as CURRENT_TIMESTAMP().
15. Preserve existing parameterized dataset/table names.
16. Do not change unrelated business columns.

Replace the current ACTN_TYP_CD block with this pattern:

CASE
  WHEN EXISTS (
    SELECT 1
    FROM `{{params.project_id}}.{{params.ds_product_pbmedh_cnf}}.T_RXBNFT_CAREMARK_PA` t
    WHERE t.event_id = v.event_id
      AND t.curr_ind = 'Y'
  )
  THEN 'U'
  ELSE 'I'
END AS ACTN_TYP_CD

Please return the corrected full BigQuery SQL and briefly list the changes made.
